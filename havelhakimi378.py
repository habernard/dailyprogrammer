def warmup1(line: list)->list:
	warmup1 = []
	for i in warmup1:
		warmup1.remove(0)

	return list(warmup1)

	num = int(input("Enter a sequence of numbers:"))
	found = []b 
	for numbers in num:
		found.append()